# Database (PostgreSQL - set automatically in Docker, override for external DB)
DATABASE_URL="postgresql://gustopro:gustopro_secret_2024@localhost:5432/gustopro?schema=public"

# Docker Compose DB password
DB_PASSWORD="gustopro_secret_2024"

# Server
PORT=3001
NODE_ENV="development"

# Email (SMTP - optional, enable when ready)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="your@gmail.com"
# SMTP_PASS="your-app-password"
# FROM_EMAIL="info@gustopro.it"
# FROM_NAME="Riva Beach Salento"
